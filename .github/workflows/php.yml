name: PHP Code Sniffer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: 'latest'
        extensions: sockets pdo_pgsql

    - name: Install Composer dependencies
      run: |
        cd /home/albertoleon/Documentos/ALURA/alura-ms/academico-php
        composer install --prefer-dist

    - name: PHPCS
      run: |
        cd /home/albertoleon/Documentos/ALURA/alura-ms/academico-php
        composer run phpcs
